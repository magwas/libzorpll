before_install:
  - sudo apt-get update -qq
  - sudo apt-get install libssl-dev libglib2.0-dev debhelper libcap-dev zlib1g-dev binutils autotools-dev po-debconf autoconf
script:
  - debuild -i -us -uc -b
